# Start Waybar before the welcome screen so the panel is visible immediately.
exec-once = /usr/bin/waybar --config /home/churros/.config/waybar/config.jsonc

exec-once = hyprpaper

exec-once = churros-welcome
