---
modules-search:
  - local

instances:
  - id: pacman-init
    module: shellprocess
    config: shellprocess-pacman.conf
  - id: fix-boot
    module: shellprocess
    config: shellprocess-fixboot.conf
  - id: churros-repo
    module: shellprocess
    config: shellprocess-repo.conf
  - id: post-install
    module: shellprocess
    config: shellprocess-cleanup.conf
  - id: grub-theme
    module: shellprocess
    config: shellprocess-grub-theme.conf
  - id: boot-nocow
    module: shellprocess
    config: shellprocess-boot-nocow.conf

sequence:
  - show:
      - welcome
      - locale
      - keyboard
      - partition
      - users
      - netinstall
      - summary
  - exec:
      - partition
      - mount
      - shellprocess@boot-nocow
      - unpackfs
      - machineid
      - fstab
      - locale
      - keyboard
      - localecfg
      - shellprocess@pacman-init
      - shellprocess@fix-boot
      - shellprocess@churros-repo
      - netinstall
      - packages
      - initcpiocfg
      - initcpio
      - users
      - networkcfg
      - hwclock
      - services-systemd
      - grubcfg
      - bootloader
      - shellprocess@grub-theme
      - shellprocess@post-install
      - umount
  - show:
      - finished

branding: churros

prompt-install: false
dont-chroot: false
oem-setup: false
disable-cancel: false
disable-cancel-during-exec: true
quit-at-end: false
